import { SearchByStatePipe } from './search-by-state.pipe';

describe('SearchByStatePipe', () => {
  it('create an instance', () => {
    const pipe = new SearchByStatePipe();
    expect(pipe).toBeTruthy();
  });
});
